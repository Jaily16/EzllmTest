{
  "annotations": {"list": []},
  "editable": false,
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "options": {"legend": {"displayMode": "list", "placement": "bottom"}},
      "targets": [{"expr": "sum(rate(ezllm_agent_commands_processed_total[5m]))", "refId": "A"}],
      "title": "Agent commands / second",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "options": {"legend": {"displayMode": "list", "placement": "bottom"}},
      "targets": [{"expr": "histogram_quantile(0.95, sum by (le, operation) (rate(ezllm_agent_graph_node_duration_milliseconds_bucket[5m])))", "refId": "A"}],
      "title": "Graph node p95",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 42,
  "tags": ["ezllm", "agent", "iteration4"],
  "templating": {"list": []},
  "time": {"from": "now-1h", "to": "now"},
  "timezone": "browser",
  "title": "EzLLM Agent Overview",
  "uid": "ezllm-agent-overview",
  "version": 1
}
