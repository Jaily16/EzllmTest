# Deprecated location: this file is a migration notice, not a runtime configuration.
# Use backend/.env.example, frontend/.env.example and observability/.env.example.
# Fill the three corresponding .env files locally; do not copy old credentials automatically.
# Select all three files by absolute CLI arguments. Nothing discovers a root .env.
# Explicit legacy --env-file / --model-env-file remain a separate deprecated mode.
# Never mix legacy source arguments with the three-file mode.
